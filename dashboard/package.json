{
  "name": "popupstore-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx prisma migrate deploy && node src/server.js",
    "dev": "node src/server.js",
    "db:migrate": "npx prisma migrate deploy",
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "express": "^4.21.0",
    "prisma": "^6.6.0",
    "uuid": "^14.0.0"
  }
}
